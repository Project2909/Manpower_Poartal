# AADHVI Technologies Manpower Allocation Portal

- Upload Excel with `Area` & `Manpower` columns
- Allocate manpower percentages
- Download updated Excel

## How to run locally

1. Clone repo
2. Run `npm install`
3. Run `npm start`
4. Open `http://localhost:3000` in browser
